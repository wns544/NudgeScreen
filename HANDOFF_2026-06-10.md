# NudgeScreen 인수인계서 - 2026-06-10

## 프로젝트

- 경로: `C:\myProgramWithCodex\screenLock`
- 앱 이름: `잠깐, 할 일`
- 패키지명: `com.wns544.nudgescreen`
- GitHub: `https://github.com/wns544/NudgeScreen.git`
- 현재 브랜치: `master`
- 원격 상태: `master`가 `origin/master`와 동기화됨

## 현재 버전

- `versionCode`: `21`
- `versionName`: `1.0.20`
- 최신 커밋: `c792b6d Bump release version to 1.0.20`

## 현재 APK

- 업로드용 APK:
  - `C:\myProgramWithCodex\screenLock\NudgeScreen-release.apk`
  - 크기: `78,536 bytes`
  - 마지막 갱신: `2026-06-10 오전 11:40:31`
- `aapt dump badging` 확인 결과:
  - package: `com.wns544.nudgescreen`
  - versionCode: `21`
  - versionName: `1.0.20`

## 오늘 처리한 핵심 내용

1. 잠금화면 점 세 개 메뉴의 자물쇠 아이콘 깨짐 수정
2. 되돌리기 아이콘 깨짐 수정
3. 할 일 잠금 상태가 다음 화면 켜기 이후에도 유지되도록 `AppSettings`에 저장
4. 진단 로그에 `Todos locked` 상태 추가
5. 되돌리기 버튼은 다시 항상 보이도록 복구
   - 삭제 항목이 있을 때만 활성 표시
   - 삭제 항목이 없을 때도 버튼 자체는 숨기지 않음
6. ONE Store 업로드 거절 원인인 낮은 `versionCode` 문제 해결
   - `19 / 1.0.18` 거절 확인
   - `20 / 1.0.19`로 올림
   - 되돌리기 버튼 복구 후 다시 `21 / 1.0.20`으로 올림

## 주요 커밋

- `c792b6d Bump release version to 1.0.20`
- `3f59c7c Keep undo button visible in lock menu`
- `4281e55 Bump release version to 1.0.19`
- `aa0b911 Fix lock menu icons and persist todo lock`
- `e8d99f4 Fix verifier screenshot capture`

## 실제 기기 검증

- A52s에서 잠금화면 커튼을 띄워 자물쇠/되돌리기 아이콘을 캡처 확인함
- 화면을 껐다 켠 뒤에도 할 일 잠금 상태가 유지되는 것을 확인함
- S23 Ultra에 ONE Store 서명 APK 설치 확인:
  - 파일: `C:\Users\wns54\Downloads\0001006287_20260610110109_signed (1).apk`
  - 설치 결과: `Success`
  - 설치 버전: `versionCode=20`, `versionName=1.0.19`
- 이후 되돌리기 버튼 복구 때문에 현재 배포 후보는 새로 만든 `21 / 1.0.20` APK임

## 현재 작업트리 상태

앱 본체 기준 변경사항 없음.

남아 있는 untracked 항목:

- `lock-compare/`
  - 실제 기기 캡처 증거 폴더
  - 앱 본체 커밋 대상 아님
- `nudge-screen-beta/`
  - 별도 베타 페이지 폴더
  - 앱 본체 커밋 대상 아님

## 다음에 할 일

1. ONE Store에 `C:\myProgramWithCodex\screenLock\NudgeScreen-release.apk` 업로드
2. 업로드 후 스토어 서명본을 내려받아 ADB로 설치 검증
3. 필요하면 S23 Ultra에서 잠금화면 메뉴를 열어:
   - 자물쇠 아이콘 표시
   - 되돌리기 버튼 표시
   - 할 일 잠금 상태 유지
   를 최종 확인

## 빌드 명령

```powershell
$env:JAVA_HOME='C:\Program Files\Android\Android Studio\jbr'
& 'C:\Users\wns54\.gradle\wrapper\dists\gradle-8.14.3-all\10utluxaxniiv4wxiphsi49nj\gradle-8.14.3\bin\gradle.bat' assembleRelease
```

## ADB 설치 명령

```powershell
& 'C:\Users\wns54\AppData\Local\Android\Sdk\platform-tools\adb.exe' install -r 'C:\myProgramWithCodex\screenLock\NudgeScreen-release.apk'
```

